<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="practice.example.forum.mapper.QuestionMapper">
  <resultMap id="BaseResultMap" type="practice.example.forum.model.Question">
    <!--
      WARNING - @mbg.generated
      This element is automatically generated by MyBatis Generator, do not modify.
      This element was generated on Sun Jan 16 10:43:10 CST 2022.
    -->
    <id column="id" jdbcType="BIGINT" property="id" />
    <result column="title" jdbcType="VARCHAR" property="title" />
    <result column="gmt_create" jdbcType="BIGINT" property="gmtCreate" />
    <result column="gmt_modified" jdbcType="BIGINT" property="gmtModified" />
    <result column="creator" jdbcType="BIGINT" property="creator" />
    <result column="comment_count" jdbcType="INTEGER" property="commentCount" />
    <result column="view_count" jdbcType="INTEGER" property="viewCount" />
    <result column="like_count" jdbcType="INTEGER" property="likeCount" />
    <result column="tag" jdbcType="VARCHAR" property="tag" />
    <result column="sticky" jdbcType="INTEGER" property="sticky" />
    <result column="description" jdbcType="VARCHAR" property="description"/>
  </resultMap>

  <sql id="Base_Column_List">
    <!--
      WARNING - @mbg.generated
      This element is automatically generated by MyBatis Generator, do not modify.
      This element was generated on Sun Jan 16 10:43:10 CST 2022.
    -->
    id, title, gmt_create, gmt_modified, creator, comment_count, view_count, like_count,
    tag, sticky, description
  </sql>

  <insert id="create" parameterType="practice.example.forum.model.Question">
    insert into
      question(title,description,gmt_create,gmt_modified,creator,tag)
    values(#{title},#{description},#{gmtCreate},#{gmtModified},#{creator},#{tag})
  </insert>

  <select id="findListQuestion" parameterType="practice.example.forum.dto.QuestionQueryDTO" resultMap="BaseResultMap">
    select
    <include refid="Base_Column_List"/>
    from question
    <where>
      <if test="search!=null and search != ''">
        and title regexp #{search}
      </if>
    </where>
    order by gmt_create desc
    limit #{offset,jdbcType=BIGINT},#{size,jdbcType=BIGINT}
  </select>

  <select id="queryByTagAllQuestion" parameterType="java.lang.String" resultMap="BaseResultMap">
    select * from question where tag = #{message}
  </select>

  <select id="getQuestionCount" resultType="java.lang.Integer" parameterType="java.lang.String">
    select count(1) from question
    <where>
      <if test="search != null and search != ''">
        and title regexp #{search}
      </if>
    </where>
  </select>

  <select id="findQuestionById" parameterType="java.lang.Integer" resultMap="BaseResultMap">
    select
    <include refid="Base_Column_List"/>
    from question where creator = #{userAccountId,jdbcType=BIGINT}
    order by gmt_create desc limit #{offset},#{size}
  </select>

  <select id="getQuestionCountById" parameterType="java.lang.Integer" resultType="java.lang.Integer">
    select count(1)
    from
      question where creator = #{userAccoundId}
  </select>

  <select id="getQuestionCountByTag" parameterType="java.lang.String" resultType="java.lang.Integer">
    select count(1)
    from
    question where tag = #{tag}
  </select>

  <select id="getById" parameterType="java.lang.Long" resultMap="BaseResultMap">
    select
    <include refid="Base_Column_List"/>
    from question where id = #{id}
  </select>

  <update id="update" parameterType="practice.example.forum.model.Question">
    update question set
                      title = #{title},
                      description = #{desctiption},
                      tag = #{tag},
                      gmt_modified = #{gmtModified},
                      where id = #{id}
  </update>

  <update id="updateToViewCount" parameterType="practice.example.forum.model.Question">
    update question
    set
      view_count = view_count + #{viewCount,jdbcType=BIGINT}
    where id = #{id,jdbcType=BIGINT}
  </update>

  <update id="updateToLikeCount" parameterType="practice.example.forum.model.Question">
    update question
    set
      like_count = like_count + #{likeCount,jdbcType=BIGINT}
    where id = #{id,jdbcType=BIGINT}
  </update>

  <update id="incCommentCount" parameterType="practice.example.forum.model.Question">
    update question
    set comment_count = comment_count + #{commentCount,jdbcType=BIGINT}
    where id = #{id,jdbcType=BIGINT}
  </update>

  <select id="selectRelated" parameterType="practice.example.forum.model.Question" resultMap="BaseResultMap">
    select * from question where id != #{id} and tag regexp #{tag}
  </select>

  <select id="findListQuestionByTag" parameterType="practice.example.forum.dto.QuestionQueryDTO" resultMap="BaseResultMap">
      select
      <include refid="Base_Column_List"/>
      from question
      <where>
        <if test="search!=null and search!=''">
          and tag regexp #{search}
        </if>
      </where>
      order by gmt_create desc
      limit #{offset,jdbcType=BIGINT},#{size,jdbcType=BIGINT}
  </select>

</mapper>